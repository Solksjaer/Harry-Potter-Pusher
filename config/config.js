// Add the appId, key, secret, cluster here.
const PusherServerConfig = {
  appId: 'appId',
  key: 'key',
  secret: 'secret',
  cluster: 'cluster',
  encrypted: true
};

// Export this module.
module.exports = PusherServerConfig;